const initialState = {
    name: 'Steve Jobs',
    email: 'steveganteng@email.com'
}

const profile = (state = initialState, action) => {
    switch(action.type) {
        default :
            return state;
    }
}

export default profile;
